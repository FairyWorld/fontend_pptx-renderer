# OOXML Geometry Source Contract

This directory contains the M0 foundation for a future spec-compiled preset geometry
engine. It is development tooling. No renderer imports these modules, and every shape still
uses the handwritten implementation in `src/shapes/presets.ts`.

## Pinned source

The authoritative input is the DrawingML geometry electronic addendum inside ECMA-376
Part 1, 5th edition (2016):

- Landing page: <https://ecma-international.org/publications-and-standards/standards/ecma-376/>
- Official package: `ECMA-376-1_5th_edition_december_2016.zip`
- Official package SHA-256:
  `9d0bcad9cf06054785b03762fcfadbf6bab7e54a5f9d69434e34b7fd464d4129`
- Nested archive: `OfficeOpenXML-DrawingMLGeometries.zip`
- Nested archive SHA-256:
  `a8517838a3a60492d267d1ff92e1ebd831adc04584de79a38a5532a68902abf6`
- `presetShapeDefinitions.xml` SHA-256:
  `2f7c868d857c1e3c4b5a6068759fe0e07d77ad58377a6618d1b02ba3507b6939`

The nested archive is stored unchanged under `vendor/` so source validation works offline.
The archive, generated catalog, and generation code are excluded from the published npm
package; the small `source-manifest.json` provenance record is included so the notice link
remains usable for package consumers. See
[`../../licenses/ECMA-text-copyright-notice.txt`](../../licenses/ECMA-text-copyright-notice.txt)
and [`../../THIRD_PARTY_NOTICES.md`](../../THIRD_PARTY_NOTICES.md).

`source-manifest.json` is the machine-readable provenance record. Changing the archive,
standard edition, or any hash requires an explicit source-upgrade review.

## Commands

```bash
pnpm geometry:generate  # validate source and regenerate the deterministic catalog
pnpm geometry:check     # fail when hashes, validation, or generated bytes differ
```

The generated catalog is a development inventory. It records each shape's guide count,
operators, path count, path commands, fill/stroke metadata, handles, connection sites, and
text-rectangle presence. It is not bundled into the renderer. The generated directory is
excluded from Prettier so `geometry:generate` remains the only byte-format authority.

Current pinned-source inventory:

- 187 top-level source entries and 186 unique shape names
- one structurally identical duplicate entry: `upDownArrow`
- 3,910 guide formulas after duplicate-shape collapse
- 319 paths across unique shapes; 63 shapes contain multiple paths
- 12 ordered guide-name rebindings
- eight explicitly registered trailing-zero formula normalizations
- all 17 ECMA guide formula operators represented

## Validation and reconciliation rules

The validator rejects malformed XML, source hash drift, unknown formula operators, wrong
arity, forward or unresolved guide references, wrong DrawingML namespaces, empty paths,
invalid path fill/stroke values, malformed path-command point counts or attributes,
non-equivalent duplicate shape entries, and stale reconciliation records.

Guide formulas are evaluated in document order. Repeating a guide name therefore means
ordered rebinding; it is retained and reported instead of being collapsed. The duplicated
`upDownArrow` elements are structurally equivalent, so the catalog retains the first and
reports the duplicate.

`source-reconciliation.json` is the only place where a pinned-source anomaly can be
normalized. M0 rejects every active `override`, including entries with plausible-looking
URLs, hashes, and case IDs. A later phase must first add an offline verifier that reads the
alternative source bytes, verifies their hash and shape definition, and resolves native
PowerPoint oracle metadata. `manualReferences` are comparison notes; CI checks their field
shape but does not treat them as verified source or oracle evidence.

The current ECMA addendum omits `upArrow`. Its status remains
`pending-native-oracle`; M0 does not choose a replacement definition.

Eight circular-arrow formulas contain an extra fourth trailing `0`. The strict parser still
requires three operands for `+-`; only the eight exact shape/guide/formula tuples in the
reconciliation file are normalized.

## Formula numeric contract

`formula-contract.mjs` locks operand order and arity for all 17 operators. The future M1
evaluator must use 60,000ths of a degree for angles and IEEE-754 binary64 intermediates,
without intermediate rounding. For PowerPoint compatibility, `sqrt x` means
`sqrt(abs(x))`, and `at2 0 0` resolves to zero. Division by zero and any non-finite result
are evaluation errors. Coordinate formatting and rounding belong to the later path emitter.
The `mod`, `sqrt`, trigonometric-angle, and `at2` compatibility behaviors link directly to
[Microsoft's implementation notes](https://learn.microsoft.com/en-us/openspecs/office_standards/ms-oi29500/851cc3e8-ab22-4f5e-86d1-23b0b06d5edd)
inside the generated contract.

The contract includes the predefined guides listed by ECMA plus `cd3`, `hd10`, `wd12`, and
`wd32`, which are used by the official electronic addendum. These four compatibility guides
are derived in the same way as the corresponding circle/height/width fractions.

## M1 entry gate

M1 may add a pure formula evaluator and geometry IR after this M0 gate remains green. It
must not route a production shape until a representative pilot has formula tests, browser
coverage, picture-clip coverage, and native PowerPoint oracle evidence. Renderer-owned
theme fill, masks, markers, effects, and resource lifecycle remain outside the geometry
engine.
